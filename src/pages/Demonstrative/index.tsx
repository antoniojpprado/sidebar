import { FC } from 'react';
import { Form } from '../../globalStyles';

const Demonstrative: FC = (props) => {
  return (
    <Form>
      Demonstrativo
    </Form>
  );
};

export default Demonstrative